name: File changes
on:
   push:
      branches:
        - main
jobs:
   create-file:
    name: Create file
    runs-on: ubuntu-latest
    steps:
       - uses: actions/checkout@v3
       - uses: actions/cache@v3
         with:
           path: tmp/layer
           key: foo
       - run: echo hello > world.txt
       - run: ls -la
      
   use-file:
    needs: create-file
    name: Use file
    runs-on: ubuntu-latest
    steps:
       - uses: actions/checkout@v3
       - uses: actions/cache@v3
         with:
           path: tmp/layer
           key: foo
       - run: cat world.txt   

   